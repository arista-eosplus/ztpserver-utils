copy flash:dhcpd.rpm extension:
no extension dhcpd.rpm     
extension dhcpd.rpm

copy installed-extensions boot-extensions  

configure

username ztpserver secret icanttellyou

man api http-commands
no protocol http
protocol https
no shutdown

interface Vlan 100
   ip address 10.0.0.1/24

no virtual-machine ztpserver
virtual-machine ZTPServer
   disk-image flash:ztpserver.vmdk
   virtual-nic 1 Vlan 100
   vnc-port 5900
   enable

no daemon ztpserver
daemon ztpserver
   command /mnt/flash/ztpserver_daemon &

write memory
